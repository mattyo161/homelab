---
# Install: ansible-galaxy collection install -r collections/requirements.yml
# See README.md for prerequisites (Ansible 8+ / ansible-core 2.15+).
collections:
  # - name: https://github.com/k3s-io/k3s-ansible.git
  #   type: git
  #   version: main

  - name: https://github.com/mattyo161/k3s-ansible.git
    type: git
    version: fix-token-initialization
